/* http://docs.angularjs.org/#!angular.widget */
angular.widget('kdp:category', function(compileElement) {
    var compiler = this;
    var category = compileElement.attr('category');
    return function(linkElement) {
	var currentScope = this;
    }
});